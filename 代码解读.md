# 幸福五子棋 · 源代码完全解读

> 本文档面向掌握 Python 基础语法的同学，逐文件、逐函数地解析整个五子棋项目的实现原理。
> 建议配合源代码文件对照阅读。

---

## 阅读指南

**建议阅读顺序**：按本文档章节顺序即可。我们从程序入口开始，先看界面（直观感受程序怎么跑），再看核心规则，最后深入 AI 算法。

**前置知识**：
- Python 基础语法（类、函数、列表、字典、集合）
- 了解递归的概念
- 不需要提前懂博弈论或机器学习，文档会从零解释

**文件总览**：

| 文件 | 一句话描述 |
|------|-----------|
| `main.py` | 程序入口，5 行代码 |
| `gui.py` | 图形界面，负责画棋盘、响应点击、调度 AI |
| `core.py` | 棋盘规则引擎，判断胜负/平局/禁手 |
| `ai.py` | 最简单的 AI，贪心策略 |
| `ai_advanced.py` | 核心 AI，Minimax 搜索 + Alpha-Beta 剪枝 |
| `ai_mcts.py` | 蒙特卡洛树搜索 AI |
| `alphazero_net.py` | 神经网络模型定义 |
| `alphazero_mcts.py` | AlphaZero 专用的 MCTS |
| `ai_alphazero.py` | AlphaZero AI 封装 |
| `ai_alphazero_train.py` | AlphaZero 训练脚本 |

---

## 第一章：程序入口 `main.py`

这是整个程序最短的文件，只有 7 行：

```python
import tkinter as tk
from gui import GomokuGUI

if __name__ == "__main__":
    root = tk.Tk()
    app = GomokuGUI(root)
    root.update_idletasks()
    root.minsize(root.winfo_width(), root.winfo_height())
    root.mainloop()
```

**逐行解析**：

| 行 | 代码 | 作用 |
|----|------|------|
| 1 | `import tkinter as tk` | 导入 Python 内置的 GUI 库 Tkinter |
| 2 | `from gui import GomokuGUI` | 从我们自己写的 `gui.py` 导入界面类 |
| 4 | `if __name__ == "__main__":` | Python 惯用写法：只有直接运行此文件时才执行下面的代码 |
| 5 | `root = tk.Tk()` | 创建主窗口对象（所有 Tkinter 程序的起点） |
| 6 | `app = GomokuGUI(root)` | 实例化我们的五子棋界面，传入主窗口 |
| 7 | `root.update_idletasks()` | 强制 Tkinter 计算窗口的实际尺寸（否则下一行拿到的是 1×1） |
| 8 | `root.minsize(...)` | 设置窗口最小尺寸，防止用户拖小窗口导致界面变形 |
| 9 | `root.mainloop()` | 启动事件循环——程序在这里"活着"，等待用户操作 |

**关键概念：事件循环（Event Loop）**

Tkinter 是事件驱动的：`mainloop()` 会不断检查"有没有鼠标点击？有没有定时器到期？"，有就执行对应的回调函数。整个程序的生命周期就是这个循环。

---

## 第二章：图形界面 `gui.py`

这是代码量最大的文件（~595 行），负责所有用户能看到和交互的东西。

### 2.1 整体架构

`gui.py` 只有一个类 `GomokuGUI`，它的职责是：
1. **画棋盘**：用 Tkinter Canvas 画网格线和棋子
2. **响应用户操作**：点击落子、悔棋、保存、回放
3. **调度 AI**：在后台线程运行 AI 计算，用动画进度条展示思考过程
4. **管理游戏模式**：PvP（人人）、PvE（人机）、EvE（机机）、Replay（回放）

### 2.2 模式系统（mode property）

```python
@property
def mode(self):
    if getattr(self, '_mode_override', None):
        return self._mode_override
    b = self.black_player_var.get()
    w = self.white_player_var.get()
    if b == "玩家" and w == "玩家": return "PvP"
    if b != "玩家" and w != "玩家": return "EvE"
    return "PvE"
```

这是一个 Python **property**（属性装饰器）。外部访问 `self.mode` 时，实际上在调用这个函数。

**逻辑**：
- 如果有 `_mode_override`（比如正在回放），直接返回它
- 否则根据黑白双方的下拉框选择自动判断模式
- 两边都是"玩家" → PvP；两边都不是"玩家" → EvE；否则 → PvE

### 2.3 `__init__`：初始化全流程

```python
def __init__(self, root):
    self.root = root
    self.root.title("幸福五子棋")
    self.game = GomokuGame()          # 创建核心引擎实例
```

初始化分为几个阶段：

1. **状态变量初始化**（第 35-50 行）：
   - `_ai_thinking`：AI 是否正在计算（用于锁定用户操作）
   - `_ai_estimated_duration`：预估 AI 思考时间（进度条动画用）
   - `flash_state`：最后一手棋的红色闪烁标记状态

2. **AI 实例化**（第 53 行）：`self._instantiate_ai()`

3. **界面布局计算**（第 55-57 行）：
   ```python
   self.cell_size = 40          # 每格 40 像素
   self.board_margin = 30       # 棋盘边距 30 像素
   self.canvas_size = 15 * 40 + 30 * 2  # = 660 像素
   ```

4. **构建界面**（第 59-61 行）：菜单栏 → UI 控件 → 应用主题

5. **触发 AI**（第 63 行）：如果黑方是 AI，立即开始思考

### 2.4 AI 创建工厂：`_create_ai_by_level`

```python
def _create_ai_by_level(self, player_color, level):
    if level == "玩家": return None
    if level == "深智(MCTS)":
        return MCTSAI(player=player_color, time_limit=5)
    elif level == "大师(M)":
        ai = AdvancedAI(player=player_color, depth=4)
        ai.vcf_enabled = self.vcf_var.get()
        return ai
    elif level == "高级(E)":
        ai = AdvancedAI(player=player_color, depth=3)
        ...
```

这是一个**工厂模式**：根据用户选择的难度字符串，创建对应的 AI 对象。所有 AI 都有统一接口 `get_best_move(game)`，所以 GUI 不需要关心具体是哪种 AI。

| 难度 | AI 类 | 关键参数 |
|------|-------|---------|
| 初级(B) | BaselineAI | 贪心策略 |
| 中级(I) | AdvancedAI | depth=2 |
| 高级(E) | AdvancedAI | depth=3 |
| 大师(M) | AdvancedAI | depth=4 |
| 深智(MCTS) | MCTSAI | time_limit=5s |
| 深度学习 | AlphaZeroAI | n_playout=400 |

<!-- PLACEHOLDER_CH2_CONTINUED -->

### 2.5 用户点击落子：`on_click` → `make_move`

当用户点击棋盘时，事件链路是：

```
用户鼠标点击 Canvas
    → on_click(event) 被触发
        → 计算点击位置对应的棋盘坐标 (r, c)
        → 调用 self.make_move(r, c)
            → 调用 self.game.make_move(r, c)  [核心引擎]
            → 画棋子
            → 检查游戏是否结束
            → 如果是 PvE 模式，触发 AI 思考
```

**坐标转换**（第 361-362 行）：
```python
c = round((event.x - self.board_margin) / self.cell_size)
r = round((event.y - self.board_margin) / self.cell_size)
```
`event.x` 是像素坐标，减去边距后除以格子大小，四舍五入得到最近的交叉点。

**防误操作守卫**（第 349-358 行）：
```python
if self._ai_thinking: return          # AI 在想，别点
if self.game.winner != 0: return      # 游戏已结束
if self.mode == "PvE" and ...: return # 不是你的回合
if self.mode == "EvE": return         # 机器对决，人不能插手
```

### 2.6 AI 异步调度：线程 + 进度条动画

这是 GUI 中最精巧的部分。问题是：AI 思考可能需要几秒，如果在主线程里算，界面会卡死。

**解决方案**：把 AI 计算放到后台线程，主线程用定时器驱动进度条动画。

```python
def ai_turn(self):
    # 1. 锁定界面
    self._ai_thinking = True
    self._ai_start_time = time.time()
    self._ai_result = None

    # 2. 在后台线程运行 AI
    def compute():
        result = ai_obj.get_best_move(self.game)
        self._ai_result = result    # 计算完毕，写入结果

    thread = threading.Thread(target=compute, daemon=True)
    thread.start()

    # 3. 启动进度条动画（主线程）
    self._animate_progress(ai_obj)
```

**进度条动画原理**（`_animate_progress`）：

```python
def _animate_progress(self, ai_obj):
    if self._ai_result is not None:   # AI 算完了？
        self._finish_ai_turn(ai_obj)  # 收尾
        return

    elapsed = time.time() - self._ai_start_time
    ratio = elapsed / self._ai_estimated_duration

    # ease-out 曲线：开始快，接近终点时减速
    if ratio < 1.0:
        progress = 90.0 * (1.0 - (1.0 - ratio) ** 2)
    else:
        progress = 90.0 + min(5.0, over * 2.0)  # 超时后缓慢爬升

    self.root.after(50, lambda: self._animate_progress(ai_obj))  # 50ms 后再次调用自己
```

这是一个**自递归定时器**：每 50ms 检查一次 AI 是否完成，同时更新进度条。`_ai_estimated_duration` 是根据最近 8 次思考耗时的加权平均预估的。

### 2.7 对称性旋转（Symmetry Breaking）

在 `_finish_ai_turn` 中有一段特殊逻辑（第 444-453 行）：

```python
if len(self.game.history) == 1 and self.game.current_player == 2:
    if random.random() < 0.75:
        # 把 AI 的应手绕黑棋第一手旋转一个随机角度
        rotated = random.choice([(dr,dc), (-dc,dr), (-dr,-dc), (dc,-dr)])
        ...
```

**为什么需要这个？** 在 EvE（机器对决）模式下，如果两个相同的 AI 对弈，它们的开局会完全一样（确定性算法）。加入随机旋转后，白方的第一步应手会有 4 种方向变化，让每局对弈都不同。

### 2.8 棋谱保存格式

```python
info_header = f"{{[C5][{header_b}][{header_w}][{res}][{now_str} 本地][DYZ-2026课程设计]"
moves_str += f";{color_char}({col_char},{row_num})"
```

保存格式示例：`{[C5][玩家][高级(E)][先手胜][2026.06.01 14:30 本地][DYZ-2026课程设计];B(H,8);W(G,7);...}`

坐标转换规则：
- 列：`c=0` → `'A'`，`c=7` → `'H'`（用 `chr(ord('A') + c)`）
- 行：`r=0` → `15`，`r=7` → `8`（用 `15 - r`，因为棋盘从上到下是 15→1）

### 2.9 棋谱回放

`parse_and_play` 解析棋谱文本，提取所有 `B(...)` 和 `W(...)` 格式的着法，然后用 `root.after(800, self.replay_next)` 每 800ms 自动落一步，形成动画效果。

### 2.10 悔棋逻辑

```python
def undo(self):
    if self.mode == "PvE":
        # 人机模式：撤销两步（你的 + AI 的），回到你上次落子前
        self.game.undo_move()
        self.game.undo_move()
    elif self.mode == "PvP":
        # 人人模式：只撤一步
        self.game.undo_move()
```

**为什么 PvE 要撤两步？** 因为你悔棋后，如果只撤一步，轮到的还是 AI，AI 会立刻再下一步。所以必须把 AI 的那步也撤掉，回到"轮到你"的状态。

### 本章小结

`gui.py` 是整个项目的"指挥中心"：
- 它不做任何棋局逻辑判断（交给 `core.py`）
- 它不做任何 AI 决策（交给 `ai_*.py`）
- 它只负责：画面 + 交互 + 调度

这就是 MVC 架构的精髓：**View（视图）和 Controller（控制器）不包含业务逻辑**。

<!-- PLACEHOLDER_CH3 -->

---

## 第三章：核心引擎 `core.py`

`core.py` 是整个项目的"物理引擎"——它定义了五子棋的规则，但完全不关心谁在下棋（人还是 AI）。

### 3.1 类总览

```python
class GomokuGame:
    _zobrist_table = None       # 类变量：所有实例共享的哈希表
    _zobrist_player_key = None  # 区分黑白方的额外哈希键
```

**实例变量**（每局游戏独立）：
| 变量 | 类型 | 含义 |
|------|------|------|
| `board` | 15×15 二维列表 | 0=空, 1=黑, 2=白 |
| `current_player` | int | 当前该谁下（1 或 2） |
| `winner` | int | 0=未结束, 1=黑赢, 2=白赢 |
| `history` | list of (r, c, player) | 落子历史记录 |
| `move_count` | int | 已落子总数（用于 O(1) 平局判定） |
| `hash` | int | 当前局面的 Zobrist 哈希值 |
| `forbidden_enabled` | bool | 是否启用禁手规则 |

### 3.2 `make_move(r, c)` — 正式落子

这是最核心的函数，每次落子都经过它：

```python
def make_move(self, r, c):
    if not self.is_valid_move(r, c) or self.winner != 0:
        return False                    # 非法落子或游戏已结束

    # 禁手检测（仅黑棋且开启时）
    if self.forbidden_enabled and self.current_player == 1:
        forbidden_msg = self.check_forbidden(r, c)
        if forbidden_msg:
            self.winner = 2             # 黑方触发禁手，白方直接获胜
            ...
            return forbidden_msg        # 返回字符串（如"三三禁手"）

    # 正常落子
    self.history.append((r, c, self.current_player))
    self.board[r][c] = self.current_player
    self.move_count += 1
    self.hash ^= self._zobrist_table[r][c][self.current_player - 1]  # 更新哈希

    if self.check_winner(r, c):
        self.winner = self.current_player
    else:
        self.current_player = 3 - self.current_player  # 1→2 或 2→1
    return True
```

**返回值设计**：
- `False`：落子失败（位置非法或游戏已结束）
- `True`：落子成功
- 字符串（如 `"三三禁手"`）：触发了禁手，GUI 会弹窗提示

### 3.3 `simulate_move` / `undo_simulate` — AI 搜索专用

```python
def simulate_move(self, r, c, player):
    """轻量模拟落子（AI搜索用），仅维护 board/hash/move_count"""
    self.board[r][c] = player
    self.move_count += 1
    self.hash ^= self._zobrist_table[r][c][player - 1]

def undo_simulate(self, r, c, player):
    """撤销 simulate_move"""
    self.board[r][c] = 0
    self.move_count -= 1
    self.hash ^= self._zobrist_table[r][c][player - 1]
```

**为什么不直接用 `make_move`？** 因为 AI 搜索时需要"假设落子→评估→撤回"数万次。`make_move` 有太多额外操作（禁手检测、history 记录、winner 判定），而 `simulate_move` 只做最少的状态维护，速度快得多。

### 3.4 Zobrist 哈希原理

**问题**：AI 的转置表需要快速判断"这个局面之前见过没有"。如果每次都把整个 15×15 棋盘转成字符串来比较，太慢了。

**Zobrist 哈希的思路**：
1. 预先为每个位置 (r, c) 和每种棋子颜色生成一个随机 64 位整数
2. 局面的哈希值 = 所有已落子位置对应随机数的 XOR

**XOR 的妙处**：
- 落子时：`hash ^= table[r][c][player]`（加入一个棋子）
- 悔棋时：`hash ^= table[r][c][player]`（同样的操作！因为 XOR 自身 = 0）
- 所以落子和悔棋都是 O(1)，不需要重新计算整个棋盘

```python
@property
def full_hash(self):
    """含当前玩家信息的完整哈希"""
    if self.current_player == 1:
        return self.hash
    return self.hash ^ self._zobrist_player_key
```

`full_hash` 额外 XOR 了一个 `_zobrist_player_key`，这样"同一个棋盘但轮到不同人下"会得到不同的哈希值——这对转置表的正确性很重要。

### 3.5 `check_winner(r, c)` — O(1) 胜负判定

```python
def check_winner(self, r, c):
    player = self.board[r][c]
    directions = [(0,1), (1,0), (1,1), (1,-1)]  # 横/竖/主对角/副对角

    for dr, dc in directions:
        count = 1
        # 向正方向数
        nr, nc = r + dr, c + dc
        while 0 <= nr < 15 and 0 <= nc < 15 and self.board[nr][nc] == player:
            count += 1
            nr += dr; nc += dc
        # 向反方向数
        nr, nc = r - dr, c - dc
        while 0 <= nr < 15 and 0 <= nc < 15 and self.board[nr][nc] == player:
            count += 1
            nr -= dr; nc -= dc
        if count >= 5:
            return True
    return False
```

**为什么是 O(1)？** 因为只检查最后落子的那个点的四个方向，每个方向最多延伸 4 格（不可能超过棋盘边界）。总共最多检查 4×8=32 个格子，与棋盘大小无关。

### 3.6 `check_forbidden(r, c)` — 禁手检测

禁手是竞技五子棋的规则：黑棋（先手方）不能形成"长连"（6 子以上）、"四四"（同时形成两个四）、"三三"（同时形成两个活三）。

```python
def check_forbidden(self, r, c):
    self.board[r][c] = 1  # 临时放上棋子

    for dr, dc in directions:
        # 提取以 (r,c) 为中心、长度为 9 的线段
        s = []
        for i in range(-4, 5):
            nr, nc = r + i*dr, c + i*dc
            if 在棋盘内: s.append(str(board[nr][nc]))
            else: s.append('2')  # 边界视为对手
        line = "".join(s)

        # 恰好五连不是禁手！
        if "11111" in line and "111111" not in line:
            self.board[r][c] = 0
            return False  # 放行

        # 检测各种禁手模式
        if "111111" in line: overs += 1      # 长连
        if any(p in line for p in four_patterns): fours += 1
        if any(p in line for p in three_patterns): open_threes += 1

    self.board[r][c] = 0  # 恢复

    if overs > 0: return "长连禁手"
    if fours >= 2: return "四四禁手"
    if open_threes >= 2: return "三三禁手"
    return False
```

**核心技巧**：把棋盘的一条线转成字符串（如 `"002111020"`），然后用 Python 的 `in` 运算符做子串匹配。这比写复杂的条件判断简洁得多。

### 本章小结

`core.py` 提供了五子棋的完整规则实现：
- `make_move` / `undo_move`：正式落子/悔棋（给 GUI 用）
- `simulate_move` / `undo_simulate`：轻量模拟（给 AI 搜索用）
- `check_winner`：O(1) 胜负判定
- `check_draw`：O(1) 平局判定（`move_count >= 225`）
- `check_forbidden`：字符串模式匹配的禁手检测
- Zobrist 哈希：O(1) 的局面唯一标识

---

## 第四章：初级 AI `ai.py`

这是最简单的 AI，只有 64 行代码，适合作为理解 AI 接口的入门。

### 4.1 策略概述

BaselineAI 的决策逻辑是一个优先级链：

```
1. 能赢吗？ → 赢！
2. 对手要赢了吗？ → 堵！
3. 都不是 → 在已有棋子附近随机下一步
4. 棋盘全空 → 随便找个位置
```

### 4.2 `get_best_move(game)` — AI 统一接口

```python
def get_best_move(self, game):
    # 1. 尝试直接赢
    best_move = self._find_winning_move(game, self.player)
    if best_move: return best_move

    # 2. 尝试堵对手
    opponent = 3 - self.player
    best_move = self._find_winning_move(game, opponent)
    if best_move: return best_move

    # 3. 在已有棋子附近下
    nearby_moves = self._get_nearby_empty_cells(game)
    if nearby_moves: return random.choice(nearby_moves)

    # 4. 兜底
    ...
```

所有 AI 类都实现这个 `get_best_move(game)` 接口，返回一个 `(r, c)` 元组。GUI 不需要知道 AI 内部怎么决策。

### 4.3 `_find_winning_move` — 暴力枚举

```python
def _find_winning_move(self, game, player_to_check):
    for r in range(15):
        for c in range(15):
            if game.board[r][c] == 0:
                game.board[r][c] = player_to_check  # 假设落子
                is_win = game.check_winner(r, c)    # 检查是否五连
                game.board[r][c] = 0                # 恢复
                if is_win: return (r, c)
    return None
```

遍历所有空位，逐个试探"如果在这里下，能不能赢"。时间复杂度 O(225)，对于简单 AI 完全够用。

### 4.4 `_get_nearby_empty_cells` — 邻近空位

```python
def _get_nearby_empty_cells(self, game):
    nearby = set()
    for r in range(15):
        for c in range(15):
            if game.board[r][c] != 0:  # 找到一个已有棋子
                for dr, dc in 八个方向:
                    nr, nc = r+dr, c+dc
                    if 在棋盘内 and 是空位:
                        nearby.add((nr, nc))
    return list(nearby)
```

**为什么不随机选全盘空位？** 因为五子棋的有效着法几乎都在已有棋子附近。在空旷区域落子毫无意义。这个简单的启发式让初级 AI 的棋也不会太离谱。

### 本章小结

BaselineAI 虽然简单，但展示了 AI 设计的核心思路：
1. **统一接口**：`get_best_move(game)` → `(r, c)`
2. **优先级决策**：先处理紧急情况（赢/堵），再考虑一般情况
3. **启发式缩小搜索范围**：只看附近的空位

它的弱点也很明显：只看一步（能不能立刻赢/被赢），完全没有"规划"能力。这就是下一章 Minimax 搜索要解决的问题。

<!-- PLACEHOLDER_CH5 -->

---

## 第五章：高级 AI `ai_advanced.py`（核心章节）

这是整个项目中最复杂、也最有技术含量的文件。它实现了一个完整的博弈搜索引擎，包含多种经典 AI 优化技术。

### 5.1 整体架构图

```
get_best_move(game)
│
├── [可选] VCF 算杀搜索（找到必胜序列直接返回）
│
└── 迭代加深搜索（depth=1, 2, 3, ... 直到时间用完）
     │
     └── 对每个候选着法：
          ├── simulate_move（模拟落子）
          ├── _minimax（递归搜索子树）
          │    ├── 转置表查询（之前算过？直接返回）
          │    ├── Alpha-Beta 剪枝（跳过不可能更好的分支）
          │    ├── Killer Move 优先（优先搜索历史上引起剪枝的着法）
          │    └── 叶子节点 → _evaluate_board（静态评估）
          └── undo_simulate（撤回）
```

### 5.2 `__init__` — 数据结构

```python
class AdvancedAI:
    def __init__(self, player, depth=3, time_limit=5.0):
        self.player = player          # 1=黑, 2=白
        self.depth = depth            # 最大搜索深度
        self.time_limit = time_limit  # 时间上限（秒）
        self.latest_win_rate = 50.0   # 最近一次评估的黑方胜率
        self.memo = {}                # 转置表（缓存已搜索的局面）
        self.vcf_enabled = False      # 是否启用 VCF 算杀
        self.killer_moves = [[None, None] for _ in range(20)]  # 每层两个 Killer
        self.history_table = [[0] * 225 for _ in range(2)]     # 历史启发表
```

| 数据结构 | 用途 | 为什么需要 |
|----------|------|-----------|
| `memo` | 转置表 | 避免重复搜索相同局面 |
| `killer_moves` | Killer Move 表 | 优先搜索"好着法" |
| `history_table` | 历史启发表 | 着法排序的辅助依据 |

### 5.3 `get_best_move` — 迭代加深主流程

**迭代加深（Iterative Deepening）** 的核心思想：先搜索 1 层，再搜索 2 层，再搜索 3 层……直到时间用完。

```python
for current_depth in range(1, self.depth + 1):
    if time.time() - self._start_time > self.time_limit * 0.8:
        break  # 时间快到了，停止加深

    # 在当前深度搜索最佳着法
    for r, c in scored:
        game.simulate_move(r, c, self.player)
        val = self._minimax(game, current_depth - 1, alpha, beta, False, r, c)
        game.undo_simulate(r, c, self.player)
        ...

    if not self._timed_out and depth_best_move:
        best_move = depth_best_move  # 更新最佳着法
```

**为什么不直接搜索最大深度？**
1. **安全网**：即使时间不够搜完 depth=4，至少有 depth=3 的结果可用
2. **更好的排序**：浅层搜索的结果可以指导深层搜索的着法排序，大幅提升剪枝效率
3. **时间控制**：可以随时中断，不会因为搜索太深而超时

### 5.4 `_minimax` — Alpha-Beta 剪枝搜索

这是整个 AI 的核心算法。先理解 Minimax 的基本思想：

**Minimax 思想**：
- 我方（Maximizer）想让分数尽可能高
- 对方（Minimizer）想让分数尽可能低
- 交替进行，形成一棵博弈树

**Alpha-Beta 剪枝**：在 Minimax 基础上，跳过"不可能影响最终决策"的分支。

```
假设我已经找到一个得分为 60 的着法（alpha=60）
现在搜索另一个分支，发现对手可以让我只得 40 分
那这个分支不可能比 60 好 → 直接剪掉，不用继续搜索
```

代码实现：

```python
def _minimax(self, game, depth, alpha, beta, is_maximizing, last_r, last_c):
    # 终止条件
    if game.winner == self.player: return 1000000 + depth   # 我赢了（越早赢越好）
    if game.winner == 3-self.player: return -1000000 - depth # 我输了
    if game.check_draw(): return 0
    if depth == 0: return self._evaluate_board(game)         # 到达叶子，静态评估

    # 转置表查询
    tt_key = game.full_hash
    if tt_key in self.memo:
        cached_depth, cached_val, flag = self.memo[tt_key]
        if cached_depth >= depth:  # 缓存的搜索更深，可以直接用
            if flag == EXACT: return cached_val
            ...

    if is_maximizing:
        max_eval = -inf
        for r, c in ordered_moves:
            game.simulate_move(r, c, self.player)
            val = self._minimax(game, depth-1, alpha, beta, False, r, c)
            game.undo_simulate(r, c, self.player)

            max_eval = max(max_eval, val)
            alpha = max(alpha, val)
            if alpha >= beta:       # Beta 剪枝！
                # 记录这个引起剪枝的着法
                self._update_killer(depth, r, c)
                break               # 不用再搜索剩余分支
        return max_eval
```

**Alpha 和 Beta 的含义**：
- `alpha`：Maximizer 目前能保证的最低得分（"我至少能拿这么多"）
- `beta`：Minimizer 目前能保证的最高得分（"对手最多让我拿这么多"）
- 当 `alpha >= beta` 时，说明当前分支不可能产生更好的结果 → 剪枝

### 5.5 转置表（Transposition Table）

**问题**：博弈树中，不同的着法顺序可能到达相同的局面。比如"先下 A 再下 B"和"先下 B 再下 A"结果一样。

**解决**：用 Zobrist 哈希作为键，缓存已搜索过的局面评估值。

```python
# 存储时
self.memo[tt_key] = (depth, max_eval, flag)

# flag 的含义：
# EXACT：这是精确值
# LOWER_BOUND：真实值 >= 这个值（发生了 Beta 剪枝）
# UPPER_BOUND：真实值 <= 这个值（发生了 Alpha 剪枝）
```

**为什么需要 flag？** 因为 Alpha-Beta 剪枝可能导致我们没有搜索完所有分支，得到的值只是一个上界或下界，不是精确值。查询时必须根据 flag 判断能否直接使用。

### 5.6 Killer Move 与 History Heuristic

这两个技术的目标相同：**让好的着法排在前面，尽早触发剪枝**。

**Killer Move**：
```python
self.killer_moves = [[None, None] for _ in range(20)]
```
每一层保存两个"杀手着法"——上次在这一层引起 Beta 剪枝的着法。下次搜索同一层时，优先尝试这些着法。

**直觉**：如果一个着法在兄弟节点引起了剪枝，它在当前节点也很可能是好着法。

**History Heuristic**：
```python
self.history_table = [[0] * 225 for _ in range(2)]
# history_table[player][r*15+c] = 累计引起剪枝的次数 × depth²
```
全局统计每个着法引起剪枝的"功劳"。功劳越大的着法，排序越靠前。乘以 `depth²` 是因为深层剪枝比浅层剪枝更有价值。

### 5.7 `_order_moves` — 着法排序

```python
def _order_moves(self, game, candidates, player, depth):
    for r, c in candidates:
        attack = self._evaluate_point(game.board, r, c, player)
        defense = self._evaluate_point(game.board, r, c, opponent)
        score = max(attack, defense) + min(attack, defense) * 0.5
        # + Killer 加分 + History 加分
    scored.sort(reverse=True)
    return scored[:max_width]  # Top-N 剪枝
```

**关键设计：`max(attack, defense)`**

为什么不是 `attack * 2 + defense`？因为那样会导致 AI 忽略防守：
- 假设某个位置对我方价值 400（弱攻击），对对手价值 6000（活三）
- 旧公式：`400*2 + 6000 = 6800`
- 如果有另一个位置对我方价值 3000：`3000*2 + 0 = 6000`
- 旧公式下 AI 会选择进攻（6800 > 6000 看似合理，但实际上不堵活三就输了）

用 `max(attack, defense)` 后：
- 防守点：`max(400, 6000) + min(400, 6000)*0.5 = 6000 + 200 = 6200`
- 进攻点：`max(3000, 0) + min(3000, 0)*0.5 = 3000`
- 防守点排在前面 ✓

### 5.8 评估函数：棋形模式表

`_evaluate_point(board, r, c, focus_player)` 评估"如果 focus_player 在 (r,c) 落子，价值多大"。

**步骤**：
1. 对四个方向（横/竖/两条对角线），提取以 (r,c) 为中心的 9 格线段
2. 模拟在中心落子
3. 将线段转为字符串（`1`=己方, `0`=空, `2`=对手）
4. 用子串匹配识别棋形
5. 累计四个方向的分数 + 检测组合威胁

**棋形分数表**：

| 棋形 | 模式示例 | 分数 | 说明 |
|------|---------|------|------|
| 成五 | `11111` | 1,000,000 | 直接获胜 |
| 活四 | `011110` | 100,000 | 两端开放，无法防守 |
| 双冲四 | 两个方向各有冲四 | +90,000 | 组合必胜 |
| 四三 | 一个冲四+一个活三 | +80,000 | 组合必胜 |
| 双活三 | 两个方向各有活三 | +70,000 | 组合必胜（非禁手） |
| 冲四 | `11110`/`11101` | 8,000 | 迫使对手防守 |
| 活三 | `01110`/`010110` | 6,000 | 一步变活四 |
| 眠三 | `211100` | 600 | 潜力较小 |
| 活二 | `001100`/`010010` | 400 | 发展空间 |
| 眠二 | `211000` | 80 | 微弱潜力 |

**组合威胁检测**：

```python
if n_fours >= 2:           total_score += 90000   # 双冲四
if n_fours >= 1 and n_open_threes >= 1: total_score += 80000  # 四三
if n_open_threes >= 2:     total_score += 70000   # 双活三
```

这些组合在五子棋中是**必胜棋形**：对手只能防一个方向，另一个方向必然成功。

### 5.9 VCF 算杀引擎

VCF（Victory by Continuous Fours）= 连续冲四取胜。思路是：

```
我下一步冲四 → 对手被迫堵 → 我再冲四 → 对手又被迫堵 → ... → 最终成五
```

```python
def find_vcf(self, game, player, depth=11):
    # 1. 对手有即时杀？必须先堵
    # 2. 我有即时杀？直接赢
    # 3. 找所有能形成冲四的点
    for r, c in candidates:
        if score >= 1000:  # 这个点能形成冲四
            game.simulate_move(r, c, player)
            # 找到对手唯一的防守点
            defense_points = self._find_forced_defenses(...)
            if len(defense_points) == 1:
                # 对手只有一个选择，模拟对手防守后递归
                game.simulate_move(defense, opponent)
                sub = self.find_vcf(game, player, depth - 2)  # 递归！
                if sub is not None:
                    return (r, c)  # 找到必胜序列
```

**为什么 depth 每次减 2？** 因为一次 VCF 循环包含"我冲四 + 对手防守"两步。depth=11 意味着最多搜索 5 轮冲四 + 最终成五。

### 5.10 `_evaluate_board` — 全盘静态评估

当搜索到达叶子节点（depth=0）时，需要对当前局面给出一个分数：

```python
def _evaluate_board(self, game):
    my_score = 0    # 我方所有棋形的总分
    opp_score = 0   # 对手所有棋形的总分

    for 每个方向:
        for 每条线段:
            score = self._line_pattern_score(线段字符串)
            归入 my_score 或 opp_score

    return my_score - opp_score * 1.3  # 防守权重略高于进攻
```

**为什么 `opp_score * 1.3`？** 因为在五子棋中，防守比进攻更紧迫——对手的活三如果不堵，下一步就变活四。1.3 的系数让 AI 稍微偏向防守，避免"只顾进攻忘了堵"的问题。

### 本章小结

`ai_advanced.py` 集成了博弈 AI 的经典技术栈：

```
迭代加深 → 保证任何时间限制下都有结果
    └── Alpha-Beta 剪枝 → 指数级减少搜索节点
         ├── 转置表 → 避免重复计算
         ├── Killer Move → 优先搜索好着法
         ├── History Heuristic → 全局着法排序
         └── 模式匹配评估 → 精确识别棋形价值
VCF 算杀 → 独立的必胜搜索通道
```

这些技术层层叠加，让 AI 在 5 秒内能搜索到 depth=4（约等于"看 4 步棋"），棋力达到业余高手水平。

<!-- PLACEHOLDER_CH6 -->

---

## 第六章：MCTS AI `ai_mcts.py`

蒙特卡洛树搜索（Monte Carlo Tree Search）是一种完全不同于 Minimax 的搜索策略。它不需要精确的评估函数，而是通过大量"模拟"来估计每个着法的胜率。

### 6.1 MCTS 四步骤

每次迭代（iteration）包含四步：

```
        根节点
       /  |  \
      A   B   C     ← 子节点（候选着法）
     / \
    D   E            ← 更深的节点

1. 选择（Selection）：从根节点沿着"最有前途"的路径向下走
2. 展开（Expansion）：到达叶子节点时，展开一个新的子节点
3. 评估（Evaluation）：对新节点的局面打分
4. 回传（Backpropagation）：把评估结果沿路径向上更新
```

重复数千次后，访问次数最多的子节点就是最佳着法。

### 6.2 节点数据结构

```python
class MCTSNode:
    __slots__ = ('parent', 'move', 'player_just_moved', 'children',
                 'wins', 'visits', 'rave_wins', 'rave_visits', 'untried_moves')
```

| 字段 | 含义 |
|------|------|
| `wins` / `visits` | 该节点的胜利次数 / 总访问次数 |
| `rave_wins` / `rave_visits` | RAVE 统计（后面解释） |
| `untried_moves` | 还没展开过的候选着法 |

`__slots__` 是 Python 优化技巧：禁止动态添加属性，减少内存占用。MCTS 会创建数万个节点，这个优化很有意义。

### 6.3 UCB1 选择公式

在"选择"阶段，如何决定走哪条路径？用 UCB1（Upper Confidence Bound）：

$$UCB1 = \frac{w_i}{n_i} + c \sqrt{\frac{\ln N}{n_i}}$$

```python
exploit = child.wins / child.visits        # 利用：这个着法的历史胜率
explore = C * sqrt(log(parent.visits) / child.visits)  # 探索：访问少的节点加分
ucb = exploit + explore
```

- **利用（Exploitation）**：选胜率高的着法
- **探索（Exploration）**：也给"没怎么试过"的着法机会

`C = 1.414`（√2）是经典取值，平衡探索与利用。

### 6.4 RAVE（Rapid Action Value Estimation）

**问题**：MCTS 初期，每个节点只被访问了几次，胜率估计很不准。

**RAVE 的思路**：如果着法 A 在搜索树的其他地方表现好，那它在当前节点可能也不错。

```python
# 选择时混合 UCB1 和 RAVE
beta = sqrt(RAVE_K / (3 * child.visits + RAVE_K))  # β 随访问次数衰减
blended = (1 - beta) * ucb + beta * rave_value
```

- 初期（visits 少）：β 大，更信任 RAVE（全局经验）
- 后期（visits 多）：β 小，更信任 UCB1（局部精确统计）

### 6.5 渐进展宽（Progressive Widening）

**问题**：五子棋每步有几十个候选着法。如果一次全部展开，搜索树太宽，每个节点分到的访问次数太少。

**解决**：子节点数量随访问次数逐渐增加：

```python
max_children = int(C * (node.visits + 1) ** 0.5)  # C=2, α=0.5
if len(node.children) < max_children and node.untried_moves:
    # 展开一个新子节点
```

访问 1 次 → 最多 2 个子节点；访问 100 次 → 最多 20 个子节点。这样搜索资源集中在最有价值的分支上。

### 6.6 启发式评估替代随机模拟

传统 MCTS 在评估阶段会"随机下完整局棋"来估计胜率。但五子棋棋盘太大（225 格），随机模拟几乎总是平局，信息量极低。

**本项目的做法**：用 AdvancedAI 的静态评估函数代替随机模拟：

```python
score = self.evaluator._evaluate_board(game)  # 借用 Minimax 的评估函数

# 用 Sigmoid 函数把分数映射到 [0, 1] 胜率空间
exponent = max(min(-score / 2000.0, 50), -50)
win_prob = 1.0 / (1.0 + math.exp(exponent))
```

Sigmoid 函数的效果：
- score = 0（均势）→ win_prob = 0.5
- score = +6000（活三优势）→ win_prob ≈ 0.95
- score = -6000（活三劣势）→ win_prob ≈ 0.05

### 6.7 优先候选生成

```python
def _get_prioritized_candidates(self, game):
    # 1. 收集半径 2 内所有空位
    # 2. 用启发式评分排序
    # 3. 只返回 Top-20
    scored.sort(reverse=True)
    return scored[:20]
```

这确保 MCTS 只在"有意义"的位置上搜索，不浪费迭代次数在远离战场的空位上。

### 本章小结

MCTS 与 Minimax 的对比：

| 特性 | Minimax (ai_advanced) | MCTS (ai_mcts) |
|------|----------------------|----------------|
| 搜索方式 | 深度优先，固定深度 | 最佳优先，时间控制 |
| 需要评估函数 | 必须（叶子节点评估） | 可选（本项目用了） |
| 剪枝方式 | Alpha-Beta | UCB1 自然聚焦 |
| 适合场景 | 评估函数准确时 | 评估函数不确定时 |
| 本项目表现 | 更强（评估函数精心设计） | 稍弱但更"创造性" |

<!-- PLACEHOLDER_CH7 -->

---

## 第七章：AlphaZero 深度学习

AlphaZero 是 DeepMind 在 2017 年提出的算法，用神经网络 + MCTS 从零学会下棋。本项目实现了一个简化版。

### 7.1 `alphazero_net.py` — 残差神经网络

**网络输入**（4 通道，15×15）：
| 通道 | 含义 |
|------|------|
| 0 | 当前玩家的棋子位置（有=1，无=0） |
| 1 | 对手的棋子位置 |
| 2 | 上一步落子位置（只有一个 1） |
| 3 | 当前玩家颜色（黑棋全 1，白棋全 0） |

**网络结构**：

```
输入 (4×15×15)
    ↓
初始卷积: Conv2d(4→128, 3×3) + BatchNorm + ReLU
    ↓
残差块 ×4:  ┌─────────────────────┐
            │ Conv(128→128) + BN + ReLU │
            │ Conv(128→128) + BN        │
            │ + 输入（跳跃连接）    + ReLU │
            └─────────────────────┘
    ↓
┌─── Policy Head ───┐    ┌─── Value Head ───┐
│ Conv(128→4, 1×1)  │    │ Conv(128→2, 1×1) │
│ + BN + ReLU       │    │ + BN + ReLU      │
│ FC → 225 维       │    │ FC → 64 → 1     │
│ LogSoftmax        │    │ Tanh             │
└───────────────────┘    └──────────────────┘
输出：每个位置的          输出：局面胜率
落子概率（对数）          [-1, 1]
```

**残差块（ResidualBlock）的关键**：

```python
class ResidualBlock(nn.Module):
    def forward(self, x):
        residual = x                    # 保存输入
        out = F.relu(self.bn1(self.conv1(x)))
        out = self.bn2(self.conv2(out))
        out += residual                 # 跳跃连接！
        return F.relu(out)
```

**为什么需要跳跃连接？** 深层网络容易出现"梯度消失"——越深的层越难学习。跳跃连接让梯度可以直接"跳过"中间层传回去，解决了这个问题。

### 7.2 `alphazero_mcts.py` — PUCT 树搜索

AlphaZero 的 MCTS 与第六章的 MCTS 有一个关键区别：**用神经网络的输出指导搜索**。

```python
def select(self, c_puct):
    # 选择 Q + U 最大的子节点
    return max(self.children.items(), key=lambda: node.get_value(c_puct))

def get_value(self, c_puct):
    # U = c_puct × P × √(父节点访问次数) / (1 + 自身访问次数)
    self.u = c_puct * self.P * sqrt(parent.n_visits) / (1 + self.n_visits)
    return self.Q + self.u
```

- `Q`：该节点的平均价值（从模拟中学到的）
- `P`：神经网络给出的先验概率（"网络觉得这步棋好不好"）
- `U`：探索奖励（访问少的节点 + 网络认为好的节点优先探索）

**与普通 MCTS 的区别**：普通 MCTS 用 UCB1（纯统计），AlphaZero 用 PUCT（统计 + 神经网络先验）。

### 7.3 `ai_alphazero.py` — 封装层

```python
class AlphaZeroAI:
    def get_best_move(self, game):
        game_copy = copy.deepcopy(game)  # 深拷贝，不影响原始游戏
        action = self.mcts_player.get_action(game_copy)
        r = action // 15
        c = action % 15
        return r, c
```

注意 AlphaZero 用一维编码表示着法：`action = r * 15 + c`。这是因为神经网络输出的是一个 225 维向量（15×15 展平）。

**降级机制**：如果没有安装 PyTorch，AlphaZero 会降级为随机落子（不会崩溃）。

### 7.4 `ai_alphazero_train.py` — 自对弈训练

训练流程：

```
循环 500 轮:
    1. 自我对弈一局（用当前网络 + MCTS 决策）
       → 记录每步的 (局面状态, MCTS概率分布, 最终胜负)
    2. 数据增广：利用棋盘对称性（旋转×4 + 翻转×2 = 8倍数据）
    3. 从数据缓冲区随机采样一批，训练网络
       - 策略损失：让网络输出接近 MCTS 的概率分布
       - 价值损失：让网络预测接近真实胜负结果
    4. 用 KL 散度自适应调整学习率
    5. 保存模型
```

**关键参数**：
| 参数 | 值 | 含义 |
|------|-----|------|
| `n_playout` | 100 | 每步 MCTS 模拟次数 |
| `game_batch_num` | 500 | 总训练轮数 |
| `batch_size` | 256 | 每次训练的样本数 |
| `buffer_size` | 10000 | 经验回放缓冲区大小 |

> 注意：当前参数是单机可接受的水平。AlphaGo Zero 论文用了 1600 次模拟 + 数百万局自对弈。

---

## 附录

### A. 五子棋棋形速查表

```
棋形名称        模式（1=己方, 0=空, 2=对手）     威胁等级
─────────────────────────────────────────────────────
成五            11111                            即时获胜
活四            011110                           必胜（无法防守）
冲四（连）      X11110 / 01111X                  迫使防守
冲四（跳）      11101 / 10111 / 11011            迫使防守
活三（连）      01110 / 011100 / 001110          一步变活四
活三（跳）      010110 / 011010                  一步变活四
眠三            211100 / 001112 / 10110          需两步成四
活二            001100 / 010010 / 01010          发展潜力
眠二            211000 / 000112                  微弱潜力

组合必胜（非禁手模式）：
  双活三 = 两个方向各有活三 → 对手只能堵一个
  四三   = 一个冲四 + 一个活三 → 对手堵四，三变四
  双冲四 = 两个方向各有冲四 → 对手只能堵一个
```

### B. 算法复杂度对比

| AI 等级 | 算法 | 时间复杂度 | 实际耗时 |
|---------|------|-----------|---------|
| 初级 | 贪心 | O(225) | <1ms |
| 中级 | Minimax depth=2 | O(25²) ≈ 625 节点 | ~50ms |
| 高级 | Minimax depth=3 | O(25³) ≈ 15000 节点 | ~0.5s |
| 大师 | Minimax depth=4 | O(15⁴) ≈ 50000 节点 | ~2s |
| 深智 | MCTS 5s | ~5000 迭代 | 5s |
| AlphaZero | 神经网络 + MCTS | 400 次推演 | ~3s |

（注：实际节点数因 Alpha-Beta 剪枝和 Top-N 限制远小于理论值）

### C. 关键参数调优指南

| 参数 | 位置 | 当前值 | 调大效果 | 调小效果 |
|------|------|--------|---------|---------|
| `depth` | ai_advanced.py | 3/4 | 更强但更慢 | 更快但更弱 |
| `time_limit` | ai_advanced.py | 5.0s | 搜索更深 | 响应更快 |
| `max_width` | ai_advanced.py | 15/25 | 不漏着法但更慢 | 更快但可能漏 |
| `time_limit` | ai_mcts.py (GUI传入) | 5s | 迭代更多更准 | 响应更快 |
| `RAVE_K` | ai_mcts.py | 300 | RAVE 影响更持久 | 更快收敛到真实值 |
| `n_playout` | ai_alphazero.py | 400 | 决策更准 | 更快 |
| 防守系数 | ai_advanced.py | 1.3 | 更保守 | 更激进 |

---

*文档完。祝答辩顺利！*
